
&НаКлиенте
Процедура ЦветПриИзменении(Элемент)
	Красный = Число(Лев(Объект.Цвет, 3));
	Зеленый = Число(Сред(Объект.Цвет, 5, 3));
	Синий = Число(Прав(Объект.Цвет, 3));
	ЦветФона = Новый Цвет(Красный, Зеленый, Синий);
	Элемент.ЦветФона = ЦветФона;
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	ЦветПриИзменении(Элементы.Цвет);
КонецПроцедуры
